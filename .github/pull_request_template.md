## Summary

<!-- What does this PR do? 1-3 bullet points. -->

## Test plan

- [ ] `python -m pytest` passes locally
- [ ] Manual smoke test on a fresh `brain init` project (if applicable)

## Checklist

- [ ] No hardcoded version strings (version lives in `pyproject.toml` only)
- [ ] No secrets or API keys in committed files
- [ ] README updated if CLI surface changed
